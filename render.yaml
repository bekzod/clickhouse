services:
  - type: pserv
    name: clickhouse
    env: docker
    plan: pro
    region: frankfurt
    autoDeploy: false
    disk:
      name: data
      mountPath: /var/lib/clickhouse
      sizeGB: 30
